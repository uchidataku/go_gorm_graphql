type Query {
    accounts: [Account!]
}

type Mutation {
    createAccount(input: CreateAccountInput!): Account
}

type Account {
    id: ID!
    name: String!
}

input CreateAccountInput {
    name: String!
}